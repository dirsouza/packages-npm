// Package usecases contains the application use-case orchestration.
// Each use case is a small, focused function injected with its dependencies
// through outbound ports — never directly through concrete types (DIP).
package usecases
